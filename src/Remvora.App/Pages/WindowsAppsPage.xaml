<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="AppsRoot"
    x:Class="Remvora.App.Pages.WindowsAppsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Remvora.App.ViewModels"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical" Spacing="4">
                <TextBlock Text="Windows Apps &amp; Bloatware" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold"/>
                <TextBlock Text="Inspect modern Windows packages, identify sponsored adware/bloatware, and safely clean unneeded apps." Style="{ThemeResource BodySubtleTextBlockStyle}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Command="{x:Bind ViewModel.RefreshCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Filter Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search (Bug 7: Centered text, 36px height) -->
            <TextBox
                Height="36"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                Padding="10,6,10,6"
                PlaceholderText="Search Windows packages..."
                Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>

            <!-- Category Filter Multi-Select DropDown (Bug 5) -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" Margin="16,0,0,0" VerticalAlignment="Center">
                <DropDownButton
                    Height="36"
                    VerticalAlignment="Center"
                    Content="{x:Bind ViewModel.FilterSummaryText, Mode=OneWay}">
                    <DropDownButton.Flyout>
                        <Flyout Placement="BottomEdgeAlignedLeft">
                            <StackPanel Spacing="8" MinWidth="190" Padding="4">
                                <TextBlock Text="Filter Packages" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                                <CheckBox Content="Bloatware Candidates" IsChecked="{x:Bind ViewModel.FilterBloatware, Mode=TwoWay}"/>
                                <CheckBox Content="Store Applications" IsChecked="{x:Bind ViewModel.FilterStore, Mode=TwoWay}"/>
                                <CheckBox Content="Provisioned Packages" IsChecked="{x:Bind ViewModel.FilterProvisioned, Mode=TwoWay}"/>
                                <MenuFlyoutSeparator Margin="0,4"/>
                                <CheckBox Content="Protected System Components" IsChecked="{x:Bind ViewModel.FilterSystem, Mode=TwoWay}"/>
                            </StackPanel>
                        </Flyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>

            <!-- Stats Badges -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Border Background="{ThemeResource SystemFillColorCautionBackgroundBrush}" CornerRadius="6" Padding="10,4">
                    <TextBlock Text="{x:Bind ViewModel.BloatwareCountText, Mode=OneWay}" Foreground="{ThemeResource SystemFillColorCautionBrush}" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Packages List -->
        <Border
            Grid.Row="2"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ListView
                ItemsSource="{x:Bind ViewModel.FilteredPackages, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:WindowsAppItemViewModel">
                        <Grid Padding="12,10" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Uninstall Package" Command="{Binding ElementName=AppsRoot, Path=ViewModel.UninstallPackageCommand}" CommandParameter="{x:Bind}" IsEnabled="{x:Bind CanUninstall}">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;"/>
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Grid.ContextFlyout>

                            <!-- Package Icon Glyph -->
                            <Border
                                Width="32" Height="32" CornerRadius="6"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                                <FontIcon Glyph="&#xE71D;" FontSize="16" Foreground="{ThemeResource AccentFillColorDefaultBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <!-- Name & Publisher & Version -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind Publisher}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="•" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                    <TextBlock Text="{x:Bind Version}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Category / Protection Badge -->
                            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind StatusBadgeText}" Style="{ThemeResource CaptionTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                            </Border>

                            <!-- Action Uninstall Button -->
                            <Button
                                Grid.Column="3"
                                Command="{Binding ElementName=AppsRoot, Path=ViewModel.UninstallPackageCommand}"
                                CommandParameter="{x:Bind}"
                                IsEnabled="{x:Bind CanUninstall}"
                                Padding="12,6"
                                VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                    <TextBlock Text="Uninstall"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Status Bar -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{x:Bind ViewModel.SummaryText, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Grid>
    </Grid>
</Page>
